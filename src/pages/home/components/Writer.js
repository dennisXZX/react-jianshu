import React, { Component } from 'react'

import { WriterWrapper } from './writerStyle'

class Writer extends Component {
  render () {
    return (
      <WriterWrapper>writer List</WriterWrapper>
    )
  }
}

export default Writer
